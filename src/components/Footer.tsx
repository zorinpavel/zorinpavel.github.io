import React from 'react';


interface IProps {
}

export const Footer = (props: IProps) => {
    const {} = props;

    return (
        <footer>
            <img src="/img/footer.png" alt="footer"/>
            <h4>Exciting space adventure!</h4>
        </footer>
    );
};
